{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "FPS Horizon dynamically adjusts the render distance based on your average FPS, keeping the game smooth without manual tweaking. When performance drops, the view distance shrinks; when FPS recovers, it expands back. Transitions are hidden by an animated distance fog that smoothly opens and closes, preventing visible chunk pop-in. Requires Sodium for optimal compatibility.",
  "authors": ["Kratos"],
  "contact": {},
  "license": "${mod_license}",
  "icon": "pack.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "pueblopaleta.FpsHorizonClient"
    ],
    "modmenu": [
      "pueblopaleta.KratosModMenuIntegration"
    ]
  },
  "mixins": [
    "fpshorizon.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "~${minecraft_version}",
    "fabric-api": "*",
    "sodium": ">=0.6.0"
  },
  "recommends": {
    "modmenu": "*",
    "cloth-config": "*"
  }
}
